[project]
name = "old-deps-test"
version = "0.1.0"
description = "Test project with deliberately outdated dependencies for upgrade-tool latency testing"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
